
class Heap:
    def __init__(self):
        print(swap)

    # def max_heapify(self, arr, index):
    #     heapSize = len(arr)
    #     left = 2 * index
    #     right = 2 * (index+1)
    #     elem = arr[index]
    #     largest = index
    #     if index < heapSize:
    #         if(arr[left] > elem):
    #             largest = right
    #         if(arr[right] > elem):
    #             largest = right
    #         if largest != index:
                # util.swap(arr, largest, index)
                # recurse
                # else:
                #     print('index {0} greater than heapsize {1}'.format(
                #         index, heapSize))

    # def min_heapify(self, node):
    #     pass

    # def createMaxHeap(self, arr):
    #     pass

    # def createMinHeap(self, arr):
    #     pass


test = Heap()
